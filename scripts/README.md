# scripts

Internal maintenance / packaging scripts used during development.
Not required to run StreetPack.

Primary entrypoint:
- `python3 streetpack_dock.py`

Ship sanity check:
- `./streetpack_shipcheck.sh check`
